name: Clan Quest Scheduler

on:
  schedule:
    - cron: '*/30 * * * *' # Runs every 30 minutes; adjust this to your preference

jobs:
  run-clan-quest:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'

      - name: Install dependencies
        run: npm install

      - name: Run handleClanQuest
        run: node index.js
